import type { TechnicianSchedule } from '@/types/technicianSchedule'
import type { Hour } from '@/types/technicianSchedule'
import { HOUR_LABELS } from './hourLabels'

export function technicianAvailableHours(
  technician: TechnicianSchedule,
  hoursLabel: Hour
) {}
